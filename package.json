{
  "name": "trpc-monorepo-eshop-express-nextjs",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ui:add": "npm run -w ui ui:add --"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.16",
    "eslint": "^8.53.0",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
